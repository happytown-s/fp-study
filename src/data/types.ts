// Re-export all types from core/types for backward compatibility
export type {
  QuestionOption,
  Question,
  AnswerRecord,
  ExamScore,
  CategoryId,
  CategoryInfo,
  QuizConfig,
  QuizCategory,
} from '../core/types';
